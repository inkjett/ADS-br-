<?xml version="1.0" encoding="utf-8"?>

<Alpha.Domain.Agent Name="ServerAlpha">   <!-- Name - имя доменного агента (опционально) -->

	<!--
		EntryPointAlphaNetAgent - точка входа агента.
			Name    - адрес (Name) агента Alpha.Net.Agent, предназначенного для взаимодействия "внешнего" мира с данным
			          доменным агентом (входящие запросы от клиентов, админисративного приложения в трее и тд.)
			Port    - значение NetEnterPort в конфигурации Alpha.Net.Agent.
			Address - IP-адрес или хост узла (опционально, по-умолчанию подразумевается IP-адрес машины, где запущена рабочая станция)
	-->
    <EntryPointAlphaNetAgent Name="local" Address="127.0.0.1" Port="1010" AlphaNetPath="AaBbCc"/>

	<!--
		Тег InstalledComponents описывает перечень установленых экземпляров компонентов на машине, где работает данный Domain-агент.
		Элементы из этого перечня "маппятся" на экземпляры компонентов в реальных ролях доменного агента.
	-->
	<InstalledComponents>
		<!--
			Тэг     - наименование типа компонента (на текущий момент поддерживается: Alpha.Server)
			Name        - имя установленного экземпляра компонента, по которому будет разрешаться маппинг в списке компонентов в конкретных ролях
			ServiceName - имя сервиса, под которым зарегистрирован экземпляр компонента
		-->
		<Alpha.Server Name="ARM" ServiceName="Alpha.Server" />
	</InstalledComponents>

	<Server>  <!-- Добавляет агенту роль сервера. -->

		<!-- Тэг Components описывает установленные компоненты, работающие в рамках роли сервера. -->
		<Components StoragePath="c:/temp/1">  <!-- StoragePath - путь к папке, в которую будут сохраняться конфигурации,
                                                                           построенные для локальных экземпляров Alpha.Server.
                                                                           Указанная папка должна существовать в файловой системе. -->
			<!--
				InstalledName - имя установленного экземпляра компонента (из перечня установленных экземпляров компонентов InstalledComponents)
				Name - имя данного Alpha.Server в проекте автоматизации
			-->
			<Component InstalledName="ARM" Name="AlphaServer" />
		</Components>
	</Server>

	<Options LoggerLevel="2"/>	<!-- Изменяет количество информации, выводимой в журнал. -->
								<!-- Больше значение - больше информации. -->
								<!-- Рекомендуемое значение - 2 -->

</Alpha.Domain.Agent>
